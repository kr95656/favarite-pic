.contents-row
  .post-title-date
    .post-title
      = @item.title
    .post-date
      = @item.created_at.strftime("%Y年%m月%d日 %H時%M分")
  .post-contents
    .post-image
      = image_tag @item.image.to_s, size: "200x200", class: 'post-contents__image' if @item.image.present?
      .more
        .span
          = image_tag 'arrow_top.png'
      %ul.more-list
        .li
          = link_to '削除', item_path(params[:id]), method: :delete
    .post-text
      - if @item.text.present?
        %p.item__text
          = @item.text
  .container
    - if current_user
      = form_for [@item, @comment] do |form|
        = form.text_area :text, class: 'container__text', placeholder: 'type a text'
        = form.submit "SEND", class: 'btn'
    - else
      %strong
        %p
          ※※※ コメントの投稿には新規登
          録/ログインが必要です ※※※
    .comments
      .h4
        コメント一覧
      - if @comments
        - @comments.each do |comment|
          .all-items--contents
            .comment-text
              = comment.text

