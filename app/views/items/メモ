%header
  -if user_signed_in?
    .user_nav-grid-6
      = link_to "マイページ",user_path(current_user.id), method: :get,  class: "btn mypage" 
      = link_to "ログアウト", destroy_user_session_path, method: :delete, class: "btn logout"
      = link_to "トップページ", root_path, class: "btn post"
  -else
    .grid-6
      = link_to "ログイン", new_user_session_path, class: "btn"
      = link_to "新規登録", new_user_registration_path, class: "btn" 
  .items-list
    %h.items-tag
      Pic LIST
    .item-tag
      = render partial: 'tags/tag', collection: @tags
.contents_row
  .container
    = form_for @item do |form|
      = render partial: 'layouts/error_message', local: { item: @item}
      %ul
        %li.post-form
          投稿フォーム
        %li.post-title
          = form.text_field :title, class: 'container__title', placeholder: 'タイトルを入力してください'
        %li.post-image
          = form.text_field :image, class: 'container__image', placeholder: "画像URLを入力してください"
        %li.post-text
          = form.text_field :text, class: 'container__text', placeholder: '一言どうぞ'
        %li.post-tag
          %ul.tag-list
            -# = form.collection_check_boxes :tag_ids, Tag.all, :id, :name, include_hidden: false do |tag| 
            -#   %li= tag.label {tag.check_box + tag.text}
            -# = form.collection_check_boxes :tag_ids, Tag.all, :id, :name do |tag| 
            = form.collection_check_boxes :tag_ids, Tag.all, :id, :name  do |tag| 
              %li.tag= tag.label {tag.check_box + tag.text }
        %li.post-send  
          = form.submit "SEND", class: 'btn'
          -# = form.submit "SEND", class: 'btn'